# Substitute @BASE_PATH@, @PORT@, and @PACKAGE@ at package installation time.
location = /webman/3rdparty/@PACKAGE@/@BASE_PATH@/@TOKEN@ {
    return 301 /webman/3rdparty/@PACKAGE@/@BASE_PATH@/@TOKEN@/;
}

location ^~ /webman/3rdparty/@PACKAGE@/@BASE_PATH@/@TOKEN@/ {
    add_header Cache-Control "no-store" always;
    add_header Referrer-Policy "no-referrer" always;
    proxy_pass http://127.0.0.1:@PORT@/@BASE_PATH@/@TOKEN@/;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 86400;
    proxy_buffering off;
}
